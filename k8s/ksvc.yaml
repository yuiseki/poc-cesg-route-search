apiVersion: serving.knative.dev/v1
kind: Service
metadata:
  name: poc-cesg-route-search
  namespace: knative-pool
spec:
  template:
    metadata:
      annotations:
        autoscaling.knative.dev/min-scale: "1"
        autoscaling.knative.dev/max-scale: "3"
        autoscaling.knative.dev/target: "3"
        autoscaling.knative.dev/scale-down-delay: "300s"
    spec:
      containerConcurrency: 3
      timeoutSeconds: 300
      nodeSelector:
        yuiseki.net/compute: "true"
      containers:
        - image: 192.168.0.90:5000/poc-cesg-route-search:0.1.2
          imagePullPolicy: Always
          ports:
            - containerPort: 8080
          env:
            - name: HOME
              value: /tmp
            - name: VALHALLA_TILE_EXTRACT_URL
              value: "http://cng-storage-http.cng-storage.svc.cluster.local/cesg-route-search/tokyo/valhalla_tiles.tar"
            - name: VALHALLA_CONFIG_URL
              value: "http://cng-storage-http.cng-storage.svc.cluster.local/cesg-route-search/tokyo/valhalla.json"
            - name: VALHALLA_LOCAL_DIR
              value: "/tmp/valhalla"
            - name: VALHALLA_LOCAL_TILE_EXTRACT
              value: "/tmp/valhalla/valhalla_tiles.tar"
            - name: VALHALLA_LOCAL_CONFIG
              value: "/tmp/valhalla/valhalla.json"
            - name: VALHALLA_ARTIFACT_MODE
              value: "tile_extract"
          resources:
            requests:
              cpu: "500m"
              memory: "1Gi"
              ephemeral-storage: "2Gi"
            limits:
              cpu: "4"
              memory: "4Gi"
              ephemeral-storage: "4Gi"
